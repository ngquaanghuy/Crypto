#ifndef CRYPTO_XOR_BASIC_H
#define CRYPTO_XOR_BASIC_H

#include "crypto/common.h"
#include "crypto/xorcode.h"

/* Internal: basic XOR + rolling XOR + bit rotation (non-protect variants).
 * Declared in xorcode.h as public API; implementations live here. */

#endif /* CRYPTO_XOR_BASIC_H */
